version: 0.2
phases:
  install:
    commands:
    npm install

  build:
    commands:
    npm run build

  post_build:
      commands:
      awc s3 sync /dist s

cache:
  paths:
    - node_modules/**/*
